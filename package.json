{
  "name": "fieldexa-website-repo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build --prefix website && node scripts/sync-dist.mjs",
    "lint": "npm run lint --prefix website",
    "preview": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.85.0"
  }
}
